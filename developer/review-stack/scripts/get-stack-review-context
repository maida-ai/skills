#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PYTHON="python3"
if ! command -v python3 >/dev/null 2>&1; then
  PYTHON="python"
fi
exec "$PYTHON" "$SCRIPT_DIR/get_stack_review_context.py" "$@"
